#include <stdio.h>

int main() {
    // Declaração de variáveis
    int i;
    
    // Loop for para iterar de 0 a 99
    for (i = 0; i < 100; i++) {
        // Imprime o valor atual de 'i'
        printf("%d\n", i);
    }
    
    return 0;
}